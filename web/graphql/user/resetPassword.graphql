mutation resetPassword(
  $resetToken: String!
  $password: String!
  $confirmPassword: String!
) {
  resetPassword(
    input: {
      resetToken: $resetToken
      password: $password
      confirmPassword: $confirmPassword
    }
  ) {
    id
    name
  }
}
